## Przykłady praktyczne

### Przykład 1: Zmiana koloru tła po kliknięciu

```html
<!DOCTYPE html>
<html>
<head>
  <title>Zmiana koloru</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 50px;
    }
    button {
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1>Kliknij przycisk, aby zmienić kolor</h1>
  <button id="zmienKolor">Zmień kolor</button>

  <script>
    const przycisk = document.getElementById("zmienKolor");
    
    przycisk.addEventListener("click", function() {
      const losowyKolor = "#" + Math.floor(Math.random()*16777215).toString(16);
      document.body.style.backgroundColor = losowyKolor;
    });
  </script>
</body>
</html>
```

### Przykład 2: Pokazywanie i ukrywanie elementu

```html
<!DOCTYPE html>
<html>
<head>
  <title>Pokazywanie/Ukrywanie</title>
  <style>
    .ukryty {
      display: none;
    }
    .box {
      width: 200px;
      height: 200px;
      background-color: coral;
      margin: 20px auto;
    }
  </style>
</head>
<body>
  <button id="toggle">Pokaż/Ukryj</button>
  <div class="box" id="box"></div>

  <script>
    const przycisk = document.getElementById("toggle");
    const box = document.getElementById("box");
    
    przycisk.addEventListener("click", function() {
      box.classList.toggle("ukryty");
    });
  </script>
</body>
</html>
```

### Przykład 3: Licznik kliknięć

```html
<!DOCTYPE html>
<html>
<head>
  <title>Licznik kliknięć</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 50px;
    }
    #licznik {
      font-size: 48px;
      font-weight: bold;
      color: darkblue;
    }
  </style>
</head>
<body>
  <h1>Licznik kliknięć</h1>
  <p id="licznik">0</p>
  <button id="kliknij">Kliknij mnie!</button>
  <button id="reset">Reset</button>

  <script>
    let count = 0;
    const licznikElement = document.getElementById("licznik");
    const przyciskKliknij = document.getElementById("kliknij");
    const przyciskReset = document.getElementById("reset");
    
    przyciskKliknij.addEventListener("click", function() {
      count++;
      licznikElement.textContent = count;
    });
    
    przyciskReset.addEventListener("click", function() {
      count = 0;
      licznikElement.textContent = count;
    });
  </script>
</body>
</html>
```

### Przykład 4: Lista zadań (TO-DO)

```html
<!DOCTYPE html>
<html>
<head>
  <title>Lista zadań</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 500px;
      margin: 50px auto;
    }
    input {
      width: 70%;
      padding: 10px;
      font-size: 16px;
    }
    button {
      padding: 10px 20px;
      font-size: 16px;
    }
    ul {
      list-style: none;
      padding: 0;
    }
    li {
      background: #f4f4f4;
      padding: 10px;
      margin: 5px 0;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <h1>Moja lista zadań</h1>
  <input type="text" id="zadanieInput" placeholder="Wpisz nowe zadanie...">
  <button id="dodajZadanie">Dodaj</button>
  <ul id="listaZadan"></ul>

  <script>
    const input = document.getElementById("zadanieInput");
    const przycisk = document.getElementById("dodajZadanie");
    const lista = document.getElementById("listaZadan");
    
    przycisk.addEventListener("click", function() {
      const tekstZadania = input.value;
      
      if (tekstZadania !== "") {
        const noweZadanie = document.createElement("li");
        noweZadanie.textContent = tekstZadania;
        lista.appendChild(noweZadanie);
        input.value = "";
      }
    });
    
    // Dodanie zadania po naciśnięciu Enter
    input.addEventListener("keypress", function(e) {
      if (e.key === "Enter") {
        przycisk.click();
      }
    });
  </script>
</body>
</html>
```

---

## Zadania do wykonania
## Uruchom zadania w przeglądarce, fragmenty html osadź w dowolnym pliku *.html

### Zadanie 1: Zmiana tekstu (Poziom: Łatwy)

**Cel:** Zmień tekst nagłówka po kliknięciu przycisku.

**HTML:**
```html
<h1 id="naglowek">Witaj!</h1>
<button id="zmien">Zmień tekst</button>
```

**Zadanie:**
1. Użyj `getElementById()`, aby pobrać element `<h1>`
2. Użyj `getElementById()`, aby pobrać przycisk
3. Dodaj nasłuchiwacz zdarzenia `click` do przycisku
4. Po kliknięciu zmień tekst nagłówka na "Tekst został zmieniony!"

---

### Zadanie 2: Zmiana koloru (Poziom: Łatwy)

**Cel:** Zmień kolor tła elementu po kliknięciu.

**HTML:**
```html
<div id="box" style="width: 200px; height: 200px; background-color: lightblue;">
  Kliknij mnie!
</div>
```

**Zadanie:**
1. Użyj `getElementById()`, aby pobrać element `div`
2. Dodaj nasłuchiwacz zdarzenia `click` do tego elementu
3. Po kliknięciu zmień `backgroundColor` na inny kolor (np. "lightcoral")

---

### Zadanie 3: Pokazywanie i ukrywanie (Poziom: Średni)

**Cel:** Stwórz przycisk, który pokazuje i ukrywa paragraf.

**HTML:**
```html
<button id="toggle">Pokaż/Ukryj</button>
<p id="tekst">To jest ukrywany tekst.</p>
```

**Zadanie:**
1. Użyj `querySelector()`, aby pobrać przycisk i paragraf
2. Dodaj klasę CSS `.ukryty` z `display: none;`
3. Po kliknięciu przycisku, użyj `classList.toggle()` do przełączania klasy `.ukryty` na paragrafie

---

### Zadanie 4: Zmiana wielu elementów (Poziom: Średni)

**Cel:** Zmień kolor wszystkich paragrafów na stronie.

**HTML:**
```html
<p class="tekst">Paragraf 1</p>
<p class="tekst">Paragraf 2</p>
<p class="tekst">Paragraf 3</p>
<button id="zmienKolor">Zmień kolor wszystkich</button>
```

**Zadanie:**
1. Użyj `querySelectorAll()`, aby pobrać wszystkie paragrafy z klasą `.tekst`
2. Użyj `getElementById()`, aby pobrać przycisk
3. Po kliknięciu przycisku, użyj pętli `forEach`, aby zmienić kolor tekstu wszystkich paragrafów na czerwony

---

### Zadanie 5: Dodawanie elementów do listy (Poziom: Średni)

**Cel:** Stwórz aplikację dodającą elementy do listy.

**HTML:**
```html
<input type="text" id="nowyElement" placeholder="Wpisz tekst...">
<button id="dodaj">Dodaj do listy</button>
<ul id="lista"></ul>
```

**Zadanie:**
1. Użyj `getElementById()`, aby pobrać input, przycisk i listę
2. Po kliknięciu przycisku:
   - Pobierz wartość z inputa (`input.value`)
   - Stwórz nowy element `<li>` używając `document.createElement()`
   - Ustaw tekst nowego elementu na wartość z inputa
   - Dodaj nowy element do listy używając `appendChild()`
   - Wyczyść input

---

### Zadanie 6: Kalkulator prostych operacji (Poziom: Trudny)

**Cel:** Stwórz prosty kalkulator dodawania dwóch liczb.

**HTML:**
```html
<input type="number" id="liczba1" placeholder="Liczba 1">
<input type="number" id="liczba2" placeholder="Liczba 2">
<button id="dodaj">Dodaj</button>
<button id="odejmij">Odejmij</button>
<p id="wynik">Wynik: </p>
```

**Zadanie:**
1. Pobierz wszystkie elementy za pomocą `getElementById()`
2. Dodaj nasłuchiwacze zdarzeń do obydwu przycisków
3. Po kliknięciu przycisku "Dodaj":
   - Pobierz wartości z obydwu inputów (pamiętaj o konwersji na liczby!)
   - Dodaj je do siebie
   - Wyświetl wynik w paragrafie
4. Po kliknięciu przycisku "Odejmij":
   - Wykonaj analogiczną operację odejmowania

**Wskazówka:** Użyj `Number()` lub `parseInt()` do konwersji tekstu na liczby.

---

### Zadanie 7: Walidacja formularza (Poziom: Trudny)

**Cel:** Stwórz prosty formularz z walidacją.

**HTML:**
```html
<form id="formularz">
  <input type="text" id="imie" placeholder="Imię">
  <input type="email" id="email" placeholder="Email">
  <button type="submit">Wyślij</button>
</form>
<p id="komunikat"></p>
```

**Zadanie:**
1. Użyj `querySelector()`, aby pobrać formularz, inputy i paragraf na komunikat
2. Dodaj nasłuchiwacz zdarzenia `submit` do formularza
3. Zapobiegnij domyślnemu działaniu formularza (`event.preventDefault()`)
4. Sprawdź czy:
   - Pole "Imię" nie jest puste
   - Pole "Email" zawiera znak "@"
5. Jeśli walidacja się powiedzie, wyświetl komunikat "Formularz wysłany!"
6. Jeśli nie, wyświetl odpowiedni komunikat błędu

---

### Zadanie 8: Projekt końcowy - Interaktywna karta (Poziom: Bardzo trudny)

**Cel:** Stwórz interaktywną kartę produktu z funkcjonalnością dodawania do koszyka.

**HTML:**
```html
<div class="karta-produktu">
  <img id="obrazek" src="produkt.jpg" alt="Produkt">
  <h2 id="nazwa">Nazwa produktu</h2>
  <p id="cena">Cena: 99 zł</p>
  <button id="zmniejsz">-</button>
  <span id="ilosc">1</span>
  <button id="zwieksz">+</button>
  <button id="dodajDoKoszyka">Dodaj do koszyka</button>
  <p id="suma">Suma: 99 zł</p>
</div>
```

**Zadanie:**
1. Pobierz wszystkie elementy używając odpowiednich metod
2. Zaimplementuj funkcjonalność:
   - Przycisk "-" zmniejsza ilość (minimum 1)
   - Przycisk "+" zwiększa ilość
   - Po każdej zmianie ilości aktualizuj sumę (ilość × cena)
   - Przycisk "Dodaj do koszyka" wyświetla alert z podsumowaniem
3. **Bonus:** Dodaj możliwość zmiany obrazka po kliknięciu
4. **Bonus:** Stylizuj kartę używając CSS i `classList`

---

## Podsumowanie

Gratulacje! Poznałeś podstawy manipulacji DOM w JavaScript:

✅ Czym jest DOM i obiekt `document`  
✅ Jak używać `getElementById()` do wyszukiwania elementów po ID  
✅ Jak używać `querySelector()` z selektorami CSS  
✅ Różnice między tymi metodami  
✅ Jak manipulować elementami (tekst, style, klasy, atrybuty)  
✅ Jak tworzyć i usuwać elementy  

### Następne kroki:

1. **Przećwicz wszystkie zadania** - praktyka czyni mistrza!
2. **Eksperymentuj** - modyfikuj przykłady i twórz własne projekty
3. **Poznaj zdarzenia** (events) - kluczowy element interaktywności
4. **Naucz się więcej o selektorach CSS** - zwiększysz swoją efektywność

### Przydatne zasoby:

- [MDN Web Docs - DOM](https://developer.mozilla.org/pl/docs/Web/API/Document_Object_Model)
- [MDN - getElementById](https://developer.mozilla.org/en-US/docs/Web/API/Document/getElementById)
- [MDN - querySelector](https://developer.mozilla.org/en-US/docs/Web/API/Document/querySelector)
- [W3Schools - JavaScript HTML DOM](https://www.w3schools.com/js/js_htmldom.asp)

---
** Miejsce do wysłania wyników pracy:
** kl VTIe: https://tiny.pl/5z5427qz
**Powodzenia w nauce JavaScript i DOM! 🚀**




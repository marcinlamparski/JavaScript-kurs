# Kurs Podstaw JavaScript - Lekcja 10

## Lekcja 10: Proste obiekty

### Czym są obiekty?

Obiekty to struktury danych, które przechowują powiązane informacje w postaci par **klucz: wartość**. Są fundamentem programowania obiektowego w JavaScript.

**Analogia:** Obiekt to jak kartoteka z opisem osoby, produktu czy dowolnej rzeczy:
- Osoba: imię, wiek, zawód
- Samochód: marka, model, rok
- Produkt: nazwa, cena, dostępność

### Tworzenie obiektów

#### 1. Object Literal (najczęstszy sposób)

```javascript
// Pusty obiekt
let pustyObiekt = {};

// Obiekt z właściwościami
let osoba = {
    imie: "Jan",
    nazwisko: "Kowalski",
    wiek: 30,
    miasto: "Warszawa"
};

console.log(osoba);
```

#### 2. Różne typy wartości

```javascript
let uzytkownik = {
    // String
    imie: "Anna",
    
    // Number
    wiek: 25,
    
    // Boolean
    aktywny: true,
    
    // Array
    hobby: ["czytanie", "bieganie", "gotowanie"],
    
    // Obiekt zagnieżdżony
    adres: {
        ulica: "Długa 5",
        miasto: "Kraków",
        kod: "30-001"
    },
    
    // Funkcja (metoda)
    przedstawSie: function() {
        console.log(`Cześć, jestem ${this.imie}`);
    }
};
```

### Dostęp do właściwości

#### Notacja kropkowa (dot notation)

```javascript
let osoba = {
    imie: "Jan",
    wiek: 30,
    miasto: "Warszawa"
};

// Odczyt
console.log(osoba.imie); // "Jan"
console.log(osoba.wiek); // 30

// Modyfikacja
osoba.wiek = 31;
console.log(osoba.wiek); // 31

// Dodawanie nowej właściwości
osoba.email = "jan@example.com";
console.log(osoba);
```

#### Notacja nawiasowa (bracket notation)

```javascript
let osoba = {
    imie: "Jan",
    wiek: 30,
    "ulubiony kolor": "niebieski" // Klucz ze spacją
};

// Odczyt
console.log(osoba["imie"]); // "Jan"
console.log(osoba["ulubiony kolor"]); // "niebieski"

// Użycie zmiennej jako klucza
let wlasciwosc = "wiek";
console.log(osoba[wlasciwosc]); // 30

// Dynamiczny dostęp
let klucz = "imie";
console.log(osoba[klucz]); // "Jan"
```

**Kiedy używać której notacji?**
- Dot notation: dla zwykłych nazw właściwości (preferowana)
- Bracket notation: gdy klucz ma spacje, cyfry na początku lub jest w zmiennej

### Metody obiektów

Metody to funkcje przypisane do właściwości obiektu.

```javascript
let kalkulator = {
    wartoscA: 10,
    wartoscB: 5,
    
    dodaj: function() {
        return this.wartoscA + this.wartoscB;
    },
    
    odejmij: function() {
        return this.wartoscA - this.wartoscB;
    },
    
    // Krótsza składnia (ES6)
    pomnoz() {
        return this.wartoscA * this.wartoscB;
    },
    
    podziel() {
        return this.wartoscA / this.wartoscB;
    }
};

console.log(kalkulator.dodaj()); // 15
console.log(kalkulator.odejmij()); // 5
console.log(kalkulator.pomnoz()); // 50
console.log(kalkulator.podziel()); // 2
```

### Słowo kluczowe `this`

`this` odnosi się do aktualnego obiektu.

```javascript
let osoba = {
    imie: "Anna",
    wiek: 28,
    
    przedstawSie() {
        console.log(`Cześć, jestem ${this.imie} i mam ${this.wiek} lat.`);
    },
    
    obchodUrodzin() {
        this.wiek++;
        console.log(`Hurra! Mam teraz ${this.wiek} lat!`);
    }
};

osoba.przedstawSie(); // "Cześć, jestem Anna i mam 28 lat."
osoba.obchodUrodzin(); // "Hurra! Mam teraz 29 lat!"
osoba.przedstawSie(); // "Cześć, jestem Anna i mam 29 lat."
```

### Iteracja po obiektach

#### for...in loop

```javascript
let produkt = {
    nazwa: "Laptop",
    cena: 3000,
    marka: "Dell",
    dostepny: true
};

// Iteracja po kluczach
for (let klucz in produkt) {
    console.log(`${klucz}: ${produkt[klucz]}`);
}

// Wynik:
// nazwa: Laptop
// cena: 3000
// marka: Dell
// dostepny: true
```

#### Object.keys(), Object.values(), Object.entries()

```javascript
let osoba = {
    imie: "Jan",
    wiek: 30,
    miasto: "Warszawa"
};

// Tablica kluczy
let klucze = Object.keys(osoba);
console.log(klucze); // ["imie", "wiek", "miasto"]

// Tablica wartości
let wartosci = Object.values(osoba);
console.log(wartosci); // ["Jan", 30, "Warszawa"]

// Tablica par [klucz, wartość]
let wpisy = Object.entries(osoba);
console.log(wpisy);
// [["imie", "Jan"], ["wiek", 30], ["miasto", "Warszawa"]]

// Praktyczne użycie z forEach
Object.entries(osoba).forEach(([klucz, wartosc]) => {
    console.log(`${klucz}: ${wartosc}`);
});
```

### Kopiowanie obiektów

```javascript
let oryginal = {
    imie: "Jan",
    wiek: 30
};

// BŁĄD: To nie kopiuje, tylko tworzy referencję!
let referencja = oryginal;
referencja.wiek = 31;
console.log(oryginal.wiek); // 31 (zmieniony!)

// POPRAWNIE: Płytkie kopiowanie (shallow copy)
// Sposób 1: Object.assign()
let kopia1 = Object.assign({}, oryginal);

// Sposób 2: Spread operator (preferowany)
let kopia2 = { ...oryginal };

kopia2.wiek = 32;
console.log(oryginal.wiek); // 31 (niezmieniony)
console.log(kopia2.wiek); // 32

// UWAGA: Płytkie kopiowanie nie kopiuje zagnieżdżonych obiektów!
let osoba = {
    imie: "Jan",
    adres: {
        miasto: "Warszawa"
    }
};

let kopia = { ...osoba };
kopia.adres.miasto = "Kraków";
console.log(osoba.adres.miasto); // "Kraków" (zmieniony!)

// Głębokie kopiowanie (deep copy) - dla zagnieżdżonych obiektów
let glebokaCopia = JSON.parse(JSON.stringify(osoba));
```

### Destrukturyzacja obiektów

```javascript
let osoba = {
    imie: "Anna",
    wiek: 25,
    miasto: "Warszawa"
};

// Tradycyjnie
let imie = osoba.imie;
let wiek = osoba.wiek;

// Destrukturyzacja (ES6)
let { imie, wiek, miasto } = osoba;
console.log(imie); // "Anna"
console.log(wiek); // 25

// Zmiana nazwy zmiennej
let { imie: personName, wiek: personAge } = osoba;
console.log(personName); // "Anna"
console.log(personAge); // 25

// Wartości domyślne
let { imie, email = "brak@email.com" } = osoba;
console.log(email); // "brak@email.com"

// Destrukturyzacja w parametrach funkcji
function przedstaw({ imie, wiek }) {
    console.log(`${imie} ma ${wiek} lat`);
}

przedstaw(osoba); // "Anna ma 25 lat"
```

### Sprawdzanie właściwości

```javascript
let osoba = {
    imie: "Jan",
    wiek: 30
};

// in operator
console.log("imie" in osoba); // true
console.log("email" in osoba); // false

// hasOwnProperty()
console.log(osoba.hasOwnProperty("imie")); // true
console.log(osoba.hasOwnProperty("email")); // false

// Sprawdzanie czy wartość nie jest undefined
console.log(osoba.imie !== undefined); // true
console.log(osoba.email !== undefined); // false
```

### Usuwanie właściwości

```javascript
let osoba = {
    imie: "Jan",
    wiek: 30,
    email: "jan@example.com"
};

// Usuwanie właściwości
delete osoba.email;
console.log(osoba); // { imie: "Jan", wiek: 30 }

// Sprawdzenie czy usunięto
console.log(osoba.email); // undefined
```

### Praktyczne przykłady

#### Przykład 1: Książka adresowa

```javascript
let kontakt = {
    imie: "Anna",
    nazwisko: "Kowalska",
    telefon: "123-456-789",
    email: "anna@example.com",
    
    pelneImie() {
        return `${this.imie} ${this.nazwisko}`;
    },
    
    wyswietlInfo() {
        console.log(`
            Imię i nazwisko: ${this.pelneImie()}
            Telefon: ${this.telefon}
            Email: ${this.email}
        `);
    }
};

kontakt.wyswietlInfo();
```

#### Przykład 2: Konto bankowe

```javascript
let konto = {
    wlasciciel: "Jan Kowalski",
    saldo: 1000,
    historia: [],
    
    wplata(kwota) {
        this.saldo += kwota;
        this.historia.push({ typ: "wpłata", kwota: kwota });
        console.log(`Wpłacono ${kwota} zł. Saldo: ${this.saldo} zł`);
    },
    
    wyplata(kwota) {
        if (kwota > this.saldo) {
            console.log("Brak środków!");
            return;
        }
        this.saldo -= kwota;
        this.historia.push({ typ: "wypłata", kwota: kwota });
        console.log(`Wypłacono ${kwota} zł. Saldo: ${this.saldo} zł`);
    },
    
    sprawdzSaldo() {
        console.log(`Saldo: ${this.saldo} zł`);
    },
    
    pokazHistorie() {
        console.log("Historia transakcji:");
        this.historia.forEach((transakcja, index) => {
            console.log(`${index + 1}. ${transakcja.typ}: ${transakcja.kwota} zł`);
        });
    }
};

konto.wplata(500); // "Wpłacono 500 zł. Saldo: 1500 zł"
konto.wyplata(200); // "Wypłacono 200 zł. Saldo: 1300 zł"
konto.sprawdzSaldo(); // "Saldo: 1300 zł"
konto.pokazHistorie();
```

#### Przykład 3: Sklep internetowy

```javascript
let produkt = {
    id: 1,
    nazwa: "Laptop Dell XPS 13",
    cena: 4500,
    kategoria: "Elektronika",
    dostepnosc: true,
    ilosc: 5,
    
    cenaPoRabacie(rabat) {
        return this.cena * (1 - rabat / 100);
    },
    
    czyDostepny() {
        return this.dostepnosc && this.ilosc > 0;
    },
    
    zakup(ilosc) {
        if (!this.czyDostepny()) {
            console.log("Produkt niedostępny!");
            return false;
        }
        if (ilosc > this.ilosc) {
            console.log(`Dostępne tylko ${this.ilosc} szt.`);
            return false;
        }
        this.ilosc -= ilosc;
        console.log(`Zakupiono ${ilosc} szt. Pozostało: ${this.ilosc}`);
        return true;
    }
};

console.log(produkt.cenaPoRabacie(10)); // 4050
produkt.zakup(2); // "Zakupiono 2 szt. Pozostało: 3"
```

### Ćwiczenia praktyczne - Lekcja 10

#### Ćwiczenie 10.1: Dane osoby
```javascript
// Stwórz obiekt 'osoba' z właściwościami:
// - imie, nazwisko, wiek, miasto
// Dodaj metody:
// - pelneImie() - zwraca imię i nazwisko
// - przedstawSie() - wyświetla "Cześć, jestem [imie] z [miasto]"
// - obchodUrodzin() - zwiększa wiek o 1
```

#### Ćwiczenie 10.2: Samochód
```javascript
// Stwórz obiekt 'samochod' z właściwościami:
// - marka, model, rokProdukcji, przebieg
// Dodaj metody:
// - pelneInfo() - zwraca string z pełną informacją
// - wiekSamochodu() - oblicza wiek (rok bieżący - rok produkcji)
// - jedz(km) - dodaje kilometry do przebiegu
```

#### Ćwiczenie 10.3: Kalkulator zaawansowany
```javascript
// Stwórz obiekt 'kalkulator' z właściwościami:
// - wynik (początkowo 0)
// Dodaj metody:
// - dodaj(x)
// - odejmij(x)
// - pomnoz(x)
// - podziel(x)
// - reset() - zeruje wynik
// - pokazWynik()

// Przykład użycia:
// kalkulator.dodaj(10);
// kalkulator.pomnoz(2);
// kalkulator.pokazWynik(); // 20
```

#### Ćwiczenie 10.4: Lista zakupów
```javascript
// Stwórz obiekt 'listaZakupow' z właściwościami:
// - produkty (tablica)
// Dodaj metody:
// - dodaj(nazwa) - dodaje produkt
// - usun(nazwa) - usuwa produkt
// - wyswietl() - pokazuje wszystkie produkty
// - czyWykonana() - sprawdza czy lista jest pusta
// - iloscProduktow() - zwraca liczbę produktów
```

#### Ćwiczenie 10.5: Quiz
```javascript
// Stwórz obiekt 'quiz' z właściwościami:
// - pytania (tablica obiektów: pytanie, odpowiedzi, poprawna)
// - punkty
// Dodaj metody:
// - zadajPytanie(numer) - wyświetla pytanie i odpowiedzi
// - sprawdzOdpowiedz(numer, odpowiedz) - sprawdza i dodaje punkt
// - pokazWynik() - wyświetla punkty/liczbę pytań
```

#### Ćwiczenie 10.6: Biblioteka
```javascript
// Stwórz obiekt 'ksiazka' z właściwościami:
// - tytul, autor, rok, przeczytana (boolean)
// Dodaj metody:
// - info() - zwraca pełną informację
// - oznaczJakoPrzeczytana()
// - wiekKsiazki()

// Stwórz tablicę z 3 książkami i:
// - Wyświetl wszystkie nieprzeczytane
// - Policz średni wiek książek
```

---

## Podsumowanie Lekcji 10

### Poznałeś:
✅ Czym są obiekty i jak je tworzyć  
✅ Właściwości obiektów (properties)  
✅ Dostęp do właściwości (dot i bracket notation)  
✅ Metody obiektów (functions as properties)  
✅ Słowo kluczowe `this`  
✅ Iterację po obiektach (for...in, Object.keys/values/entries)  
✅ Kopiowanie obiektów (shallow i deep copy)  
✅ Destrukturyzację obiektów  
✅ Sprawdzanie i usuwanie właściwości  

### Najważniejsze zasady:
- Obiekty grupują powiązane dane i funkcjonalność
- `this` odnosi się do bieżącego obiektu
- Obiekty są przekazywane przez referencję (nie przez wartość)
- Używaj spread operator `{...}` do płytkiego kopiowania
- Metody to funkcje wewnątrz obiektów

### Dobre praktyki:
✅ Używaj camelCase dla nazw właściwości  
✅ Grupuj powiązane dane w obiekty  
✅ Używaj metod do manipulacji właściwościami  
✅ Dot notation dla standardowych nazw  
✅ Bracket notation gdy klucz jest dynamiczny  

### Typowe błędy:
❌ Próba kopiowania obiektu przez `=` (tworzy referencję!)  
❌ Zapominanie o `this` w metodach  
❌ Używanie arrow functions jako metod (problem z `this`)  
❌ Mylenie obiektów z tablicami  

---

## GRATULACJE! 🎉

### Ukończyłeś podstawowy kurs JavaScript!

Teraz znasz:
- ✅ Zmienne i typy danych
- ✅ Operatory
- ✅ Stringi i ich metody
- ✅ Tablice i metody tablicowe
- ✅ Instrukcje warunkowe
- ✅ Pętle
- ✅ Funkcje
- ✅ Obiekty

### Jesteś gotowy do nauki DOM!

**Co dalej?**
1. **Przećwicz wszystkie ćwiczenia** - nie przechodź dalej bez solidnych podstaw
2. **Zbuduj mini-projekty** - użyj poznanych umiejętności
3. **Zacznij naukę DOM** - manipulacja dokumentem HTML
4. **Ucz się dalej** - eventy, formularze, API

**Pamiętaj:** Programowanie to umiejętność praktyczna. Im więcej kodu napiszesz, tym lepszy się staniesz!

**Powodzenia w dalszej nauce! 🚀**